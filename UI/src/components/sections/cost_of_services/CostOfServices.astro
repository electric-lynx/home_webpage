---
import PackageCard from "@components/blocks/cards/PackageCard.astro";
import Title from "@components/shared/Title.astro";
import { Text, Container } from "@components/shared";

import { packages } from "./data/servicesPrices.json";

const availablePackages = packages.filter(
  (clientPackages) => clientPackages.status === "available"
);
---

<section id="pricing">
  <Container className="flex flex-col midmd:flex-row gap-10 lg:gap-12">
    <div class="flex-1 flex midmd:w-7/12 lg:w-1/2 flex-col">
      <div class="text-center max-w-3xl mx-auto space-y-4">
        <Title>Pricing</Title>
        <Text tag="p" className="mt-8">
          Standard packages to upgrade your business; custom solutions for
          complex problems.
        </Text>
        <Text tag="p" className="mt-8">
          The internet is filled with sub-par websites. We want to change that.
          Each of our websites is custom-made, fully-polished and built around
          your brand. Primarly focus on web content,
        </Text>
      </div>
      <div class="pt-8">
        {
          availablePackages.map((lynxpackage) => {
            return (
              <PackageCard
                icon={lynxpackage.icon}
                title={lynxpackage.title}
                description={lynxpackage.description}
                included={lynxpackage.included}
                price={lynxpackage.price}
              />
            );
          })
        }
      </div>
    </div>
  </Container>
</section>
